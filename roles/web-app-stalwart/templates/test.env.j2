ADMIN_EMAIL={{ lookup('users', 'administrator').email | dotenv_quote }}

ADMIN_IMAP_PASSWORD={{ lookup('users', 'administrator').password | dotenv_quote }}

BIBER_EMAIL={{ lookup('users', 'biber').email | dotenv_quote }}

BIBER_IMAP_PASSWORD={{ lookup('users', 'biber').password | dotenv_quote }}

MAILU_MAILDIR_VOLUME={{ lookup('volume', 'web-app-mailu', 'dovecot_mail').name | dotenv_quote }}

MAIL_DOMAIN={{ STALWART_DOMAIN | dotenv_quote }}

PYTHON_BIN={{ ansible_python_interpreter | dotenv_quote }}

REPO_SRC_DIR={{ playbook_dir | dotenv_quote }}

{# Exception: the state machine drives nested full deploys of this stack — stack host of a compose deploy only. #}
STALWART_MIGRATION_E2E={{ (('web-app-mailu' in group_names) and DEPLOYMENT_MODE == 'compose' and IS_STACK_HOST | bool) | string | lower }}

TEST_INVENTORY_DIR={{ inventory_dir | dotenv_quote }}
