---
local:
  # Exception: not the /28 this role used to hold — the swarm branch renumbered every
  # role to a per-role /24, and mailu took the whole 192.168.106.0/24 that /28 sat inside.
  subnet: 192.168.108.0/24
