---
- name: "🛡️ Apply the Tor egress guard"
  listen: apply tor egress guard
  ansible.builtin.systemd_service:
    name: "{{ TOR_EGRESS_GUARD_UNIT | basename }}"
    state: restarted
    daemon_reload: true
  become: true
