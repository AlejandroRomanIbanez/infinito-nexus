---
matomo:
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
# nocheck: shared
# nocheck: dynamic-flag
redis:
  backup:
    disabled: true
  enabled: true
  shared: "{{ 'svc-db-redis' in group_names }}"
postgres:
  enabled: true # nocheck: dynamic-flag
  shared: "{{ 'svc-db-postgres' in group_names }}"
onlyoffice:
  # nocheck: default-placement-manager  embedded postgres/redis/rabbitmq engine state; node-local in swarm
  default_placement: manager
  enabled: true
  shared: true
  image: onlyoffice/documentserver
  version: latest
  name: onlyoffice
  min_storage: 20GB
  cpus: 2
  mem_reservation: 1g
  mem_limit: 4g
  pids_limit: 8192
  backup:
    no_stop_required: true
  ports:
    local:
      http: 8061
    internal:
      http: 80
  proxy:
    service_key: onlyoffice
  lifecycle: beta
css:
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
prometheus:
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"
