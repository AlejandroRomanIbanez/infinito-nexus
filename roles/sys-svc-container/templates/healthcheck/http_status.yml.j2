{% set container_port = container_port | default(lookup('config', application_id, 'services.' ~ service_name ~ '.ports.internal.http', '')) %}
    healthcheck:
      test:
        - "CMD"
        - "bash"
        - "-c"
        - "exec 3<>/dev/tcp/localhost/{{ container_port }} && echo -e 'GET /{{ container_healthcheck }} HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && head -n1 <&3 | grep -qE '^HTTP/1\\.[01] [23][0-9][0-9]'"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: {{ container_healthcheck_start_period | default('30s') }}
{{ "\n" }}
