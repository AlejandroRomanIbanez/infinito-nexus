---
# No exception logic required: the key the app actually reads (instances) is set generically
# by the config SPOT (meta/addons/integration_peertube.yml plugin_configuration + tasks/plugin);
# per-user OAuth is authorised at runtime. Coupling is verified by the Playwright spec.
