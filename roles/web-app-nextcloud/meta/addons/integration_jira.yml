---
enabled: "{{ (lookup('api', 'jira.client_id') | length > 0) and (lookup('api', 'jira.client_secret') | length > 0) }}"
required: true
mechanism: plugin
source: upstream
config:
  plugin_configuration:
    - appid: "integration_jira"
      configkey: "client_id"
      configvalue: "{{ lookup('api', 'jira.client_id') }}"
    - appid: "integration_jira"
      configkey: "client_secret"
      configvalue: "{{ lookup('api', 'jira.client_secret') }}"
