{% include 'roles/sys-svc-compose/templates/base.yml.j2' %}

  baserow:
{% include 'roles/sys-svc-container/templates/base.yml.j2' %}
    {{ lookup('container_image', application_id, 'baserow') }}
    {{ lookup('compose_only', 'container_name', BASEROW_CONTAINER) }}
{{ lookup('container_volumes', application_id, 'baserow') | indent(4) }}
    ports:
      - "{{ DOCKER_BIND_HOST }}:{{ lookup('config', application_id, 'services.baserow.ports.local.http') }}:80"
    {{ lookup('container_networks') | indent(4) }}
{{ lookup('container_depends_on', application_id) }}

{{ lookup('compose_volumes', application_id) }}
{{ lookup('compose_networks') }}
